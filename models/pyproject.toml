[project]
name = "restaurant-models"
version = "0.1.0"
requires-python = "==3.14.*"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/restaurant_models"]