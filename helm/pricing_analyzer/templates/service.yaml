apiVersion: v1
kind: Service
metadata:
  name: {{ include "pricing_analyzer.fullname" . }}
  labels:
    {{- include "pricing_analyzer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pricing_analyzer.selectorLabels" . | nindent 4 }}
