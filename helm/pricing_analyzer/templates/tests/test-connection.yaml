apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pricing_analyzer.fullname" . }}-test-connection"
  labels:
    {{- include "pricing_analyzer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pricing_analyzer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
