apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "supply_gap_analyzer.fullname" . }}-test-connection"
  labels:
    {{- include "supply_gap_analyzer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "supply_gap_analyzer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
