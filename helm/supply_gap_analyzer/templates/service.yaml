apiVersion: v1
kind: Service
metadata:
  name: {{ include "supply_gap_analyzer.fullname" . }}
  labels:
    {{- include "supply_gap_analyzer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "supply_gap_analyzer.selectorLabels" . | nindent 4 }}
