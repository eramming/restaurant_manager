apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sales_inventory_manager.fullname" . }}-test-connection"
  labels:
    {{- include "sales_inventory_manager.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sales_inventory_manager.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
