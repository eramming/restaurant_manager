apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: riis-cluster
  region: us-east-2

managedNodeGroups:
  - name: workers
    instanceType: t3.micro
    minSize: 1
    desiredCapacity: 1
    maxSize: 2

addons:
  - name: eks-pod-identity-agent